# Dumpster Back-end

Back-end for the Dumpster system.

## Instructions

### Install the dependencies

```bash
$ npm install
```

### Run the server

```bash
$ PORT=<port> npm start
```

The app will be served at `localhost:3000`.
